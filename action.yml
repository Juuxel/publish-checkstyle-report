name: "Publish Checkstyle/CodeNarc Report"
description: "Annotates files with Checkstyle or CodeNarc log outputs."
inputs:
  reports:
    description: "The glob paths to all report XML files"
    required: true
runs:
  using: node24
  main: index.js
branding:
  color: yellow
  icon: check-square
